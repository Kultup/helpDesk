# Налаштування AI Провайдерів (Groq та OpenAI)

## Огляд

Система HelpDesk підтримує два AI провайдери:
- **Groq** - швидка, безкоштовна AI платформа (рекомендовано для початку)
- **OpenAI** - платформа ChatGPT (потужніша, але платна)

Ви можете вибирати між ними в налаштуваннях бота через веб-інтерфейс.

## 📦 Що було додано

### Backend
1. **`backend/services/openaiService.js`** - сервіс для роботи з OpenAI API
2. **`backend/services/aiService.js`** - універсальний роутер AI запитів
3. **`backend/models/AIApiUsage.js`** - універсальна модель для трекінгу використання API
4. **`backend/models/BotSettings.js`** - оновлено з полями:
   - `aiProvider` - вибір провайдера ('groq' або 'openai')
   - `openaiApiKey` - API ключ OpenAI
   - `openaiModel` - модель OpenAI (gpt-4o-mini, gpt-4o, тощо)
5. **`backend/migrations/addAIProviderField.js`** - міграція для існуючих налаштувань

### Frontend
1. **`frontend/src/pages/BotSettings.tsx`** - оновлено UI з вибором провайдера

## 🚀 Початкове налаштування

### 1. Встановлення залежностей

OpenAI SDK вже включено в `package.json`. Якщо ви щойно клонували проект, встановіть залежності:

```bash
cd backend
npm install
```

### 2. Запуск міграції

Запустіть міграцію для оновлення існуючих налаштувань:

```bash
cd backend
node migrations/addAIProviderField.js
```

### 3. Налаштування .env (опціонально)

Ви можете додати змінні в `.env`, але **рекомендується налаштовувати через веб-інтерфейс**:

```env
# Вибір AI провайдера (groq або openai)
AI_PROVIDER=groq

# Groq налаштування
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.3-70b-versatile

# OpenAI налаштування
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini
```

## 🌐 Налаштування через веб-інтерфейс

### Крок 1: Відкрийте налаштування бота

1. Увійдіть в систему HelpDesk
2. Перейдіть до **Налаштування** → **Налаштування AI асистента**

### Крок 2: Виберіть провайдера

У полі "AI Провайдер" виберіть:
- **Groq** - для безкоштовного використання
- **OpenAI** - для використання ChatGPT моделей

### Крок 3: Налаштуйте API ключ

#### Для Groq:
1. Отримайте безкоштовний API ключ на [console.groq.com/keys](https://console.groq.com/keys)
2. Вставте ключ у поле "Groq API Key"
3. Виберіть модель (рекомендовано: **Llama 3.3 70B**)

#### Для OpenAI:
1. Отримайте API ключ на [platform.openai.com/api-keys](https://platform.openai.com/api-keys)
2. Вставте ключ у поле "OpenAI API Key"
3. Виберіть модель:
   - **gpt-4o-mini** - економічна, рекомендована (найдешевша)
   - **gpt-4o** - найновіша, потужна
   - **gpt-4-turbo** - швидка, потужна
   - **gpt-3.5-turbo** - базова

### Крок 4: Увімкніть AI асистента

Поставте галочку "Увімкнути AI асистента в Telegram боті" та натисніть **Зберегти**.

## 💰 Вартість та ліміти

### Groq (безкоштовно)
- ✅ **Безкоштовний план**
- 14,400 запитів на день (RPD)
- 7,000 токенів за хвилину (TPM)
- Швидкість відповіді: ~0.5-2 секунди

### OpenAI (платно)
- ⚠️ **Платний сервіс** - потрібна оплата
- Ціни (станом на лютий 2024):
  - **gpt-4o-mini**: $0.15 / 1M вхідних токенів, $0.60 / 1M вихідних токенів
  - **gpt-4o**: $2.50 / 1M вхідних токенів, $10.00 / 1M вихідних токенів
  - **gpt-4-turbo**: $10.00 / 1M вхідних токенів, $30.00 / 1M вихідних токенів
  - **gpt-3.5-turbo**: $0.50 / 1M вхідних токенів, $1.50 / 1M вихідних токенів

**Приблизна вартість** (для 1000 запитів з середньою довжиною):
- gpt-4o-mini: ~$0.10-0.30
- gpt-4o: ~$2-5
- gpt-3.5-turbo: ~$0.05-0.15

## 🔄 Перемикання між провайдерами

Ви можете легко перемикатися між Groq та OpenAI:

1. Відкрийте **Налаштування AI асистента**
2. Змініть значення "AI Провайдер"
3. Натисніть **Зберегти**

Зміни застосовуються негайно без перезавантаження сервера.

## 🧪 Тестування

Перевірте роботу AI через Telegram бота:

1. Напишіть боту будь-яке запитання (не команду)
2. Бот має відповісти використовуючи обраний AI провайдер
3. Перевірте статистику використання API в налаштуваннях

## 📊 Моніторинг використання

### Через Telegram
Надішліть боту команду:
```
/ai_stats
```

### Через веб-інтерфейс
Відкрийте **Налаштування** → **Статистика AI**

## 🔧 Архітектура

```
┌─────────────────────────────────────────┐
│         Telegram Bot / Web UI           │
└──────────────────┬──────────────────────┘
                   │
                   ▼
         ┌──────────────────┐
         │   aiService.js   │ ◄─── Універсальний роутер
         └──────────────────┘
                   │
         ┌─────────┴─────────┐
         ▼                   ▼
  ┌──────────────┐   ┌──────────────┐
  │ groqService  │   │ openaiService│
  └──────────────┘   └──────────────┘
         │                   │
         ▼                   ▼
  ┌──────────────┐   ┌──────────────┐
  │   Groq API   │   │  OpenAI API  │
  └──────────────┘   └──────────────┘
```

## 📝 Основні файли

### Backend сервіси
- `backend/services/aiService.js` - головний роутер AI запитів
- `backend/services/groqService.js` - Groq AI сервіс
- `backend/services/openaiService.js` - OpenAI сервіс
- `backend/services/telegramService.js` - інтеграція з Telegram (використовує aiService)

### Моделі
- `backend/models/BotSettings.js` - налаштування бота (включає AI параметри)
- `backend/models/AIApiUsage.js` - трекінг використання API

### Controllers та Routes
- `backend/controllers/settingsController.js` - контролер налаштувань
- `backend/routes/analytics.js` - AI аналітика та звіти
- `backend/routes/tickets.js` - AI аналіз тікетів
- `backend/routes/knowledgeBase.js` - генерація FAQ

### Frontend
- `frontend/src/pages/BotSettings.tsx` - UI налаштувань AI

## ⚠️ Важливі примітки

1. **Безпека API ключів**: 
   - Ніколи не комітьте API ключі в Git
   - API ключі зберігаються в MongoDB зашифровано
   - Веб-інтерфейс показує маскований ключ (******)

2. **Groq vs OpenAI**:
   - Groq - швидший, безкоштовний, обмежені можливості
   - OpenAI - потужніший, платний, більше функцій

3. **Міграція існуючих налаштувань**:
   - Скрипт `migrations/addAIProviderField.js` автоматично додає поля
   - За замовчуванням встановлюється `aiProvider: 'groq'`

4. **Зворотна сумісність**:
   - Groq сервіс продовжує працювати як раніше
   - Всі існуючі API виклики роутяться через aiService

## 🐛 Troubleshooting

### Помилка: "AI асистент вимкнено"
- Перевірте чи увімкнено AI в налаштуваннях
- Переконайтесь що API ключ введено правильно

### Помилка: "OpenAI API key is invalid"
- Перевірте формат ключа (має починатися з `sk-`)
- Переконайтесь що ключ активний на platform.openai.com
- Перевірте баланс акаунту OpenAI

### Groq не відповідає
- Перевірте інтернет-з'єднання
- Перевірте ліміти API на console.groq.com
- Переконайтесь що ключ валідний

### Як перевірити який провайдер активний?
Подивіться в логах:
```bash
pm2 logs helpdesk-backend | grep "AI Provider"
```

Або через Telegram надішліть `/ai_stats`

## 📚 Додаткові ресурси

- [Groq Documentation](https://console.groq.com/docs)
- [OpenAI API Documentation](https://platform.openai.com/docs)
- [Groq Console](https://console.groq.com)
- [OpenAI Platform](https://platform.openai.com)

## 🎉 Готово!

Тепер ви можете використовувати як Groq, так і OpenAI для AI асистента в HelpDesk системі!
