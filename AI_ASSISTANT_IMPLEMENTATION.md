# Реалізація AI Асистента в Telegram Боті

## Загальна архітектура

AI асистент реалізований через **Groq API** (LLM модель) та інтегрований в Telegram бот для надання інтелектуальних відповідей користувачам.

## Компоненти системи

### 1. **GroqService** (`backend/services/groqService.js`)

Основний сервіс для роботи з AI через Groq API.

#### Ініціалізація
- Використовує API ключ з `BotSettings` (таблиця `botsettings`)
- Перевіряє налаштування `aiEnabled` для активації/деактивації
- Модель за замовчуванням: `llama-3.3-70b-versatile`
- Ініціалізується при старті бота

#### Основні методи:

**`getAIResponse(userMessage, conversationHistory, context)`**
- Генерує відповідь на повідомлення користувача
- Використовує історію розмови (до 10 останніх повідомлень)
- Інтегрує контекст:
  - **AI Knowledge Base** - пошук релевантних статей з бази знань
  - **Тікети користувача** - інформація про останні 5 заявок
- Системний промпт налаштовується через `BotSettings.aiSystemPrompt`

**`analyzeIntent(userMessage)`**
- Аналізує намір користувача
- Визначає, чи користувач хоче створити тікет
- Витягує дані для тікета:
  - `title` - заголовок проблеми
  - `description` - опис проблеми
  - `priority` - пріоритет (low, medium, high, urgent)
  - `category` - категорія (Hardware, Software, Network, Access, Other)
  - `sentiment` - тональність (positive, neutral, negative)
  - `ticketType` - тип (incident, request)
  - `confidence` - впевненість AI (0-1)
- Використовує JSON формат відповіді для точності

**`transcribeAudio(filePath)`**
- Транскрибує голосові повідомлення через Groq Whisper
- Модель: `whisper-large-v3`
- Мова: українська (пріоритет)

### 2. **TelegramService** (`backend/services/telegramService.js`)

Інтеграція AI в Telegram бот.

#### Обробка повідомлень

**`handleAIChat(msg, user)`** - основна функція обробки AI чату:

1. **Аналіз наміру**
   - Викликає `groqService.analyzeIntent()` для визначення наміру
   - Перевіряє ключові слова для створення тікета
   - Якщо `isTicketIntent === true` та `confidence > 0.6` → запускає створення тікета

2. **Автоматичне створення тікета**
   - Якщо AI розпізнав намір створити тікет:
     - Створює сесію з даними тікета (title, description, priority)
     - Автоматично підвищує пріоритет, якщо sentiment = 'negative'
     - Пропонує користувачу додати фото
     - Переходить до процесу створення тікета

3. **Звичайна AI відповідь**
   - Якщо це не намір створити тікет:
     - Отримує історію розмови (до 10 повідомлень)
     - Отримує останні 5 тікетів користувача для контексту
     - Викликає `groqService.getAIResponse()` з контекстом
     - Відправляє відповідь користувачу
     - Зберігає повідомлення в історію розмови
     - Показує кнопки швидких дій:
       - 📝 Створити тікет
       - 📋 Мої тікети
       - 🔄 Головне меню

#### Інтеграція з голосовими повідомленнями

**`handleVoice(msg, user)`**
- Отримує голосове повідомлення
- Завантажує аудіофайл
- Викликає `groqService.transcribeAudio()` для розпізнавання
- Показує розпізнаний текст користувачу
- Передає текст в `handleAIChat()` для обробки

#### Умови активації AI

AI асистент активується, якщо:
- `groqService.isEnabled()` повертає `true`
- Користувач зареєстрований та активний
- Повідомлення не є командою (не починається з `/`)
- Немає активної сесії (створення тікета, реєстрація тощо)

## Системний промпт

### Базовий промпт (якщо не налаштовано в БД):

```
Ви - інтелектуальний асистент системи HelpDesk (@Kultup_bot). 
Ваша мета - допомагати користувачам вирішувати технічні питання та надавати інформацію про їхні заявки.

ОСНОВНІ ПРАВИЛА:
1. МОВА: Спілкуйтеся виключно українською мовою
2. ПЕРЕКЛАД: Якщо користувач пише іншою мовою, перекладіть на українську
3. ТОН: Будьте ввічливим, професійним, лаконічним та емпатичним
4. БАЗА ЗНАНЬ: Використовуйте інформацію з бази знань для відповіді

ФУНКЦІОНАЛ БОТА:
- 📝 Створити заявку: Команда /create або описати проблему
- 📋 Мої заявки: Команда /tickets
- 🏠 Головне меню: Команда /start або /menu
- 🎤 Голосові: Бот розуміє голосові повідомлення
- 📸 Фото: Можна додавати фото до заявок

ОБМЕЖЕННЯ:
- Ви НЕ можете змінювати статус заявок
- Ви НЕ можете змінювати паролі
- Ви НЕ бачите повідомлень інших користувачів
```

### Додатковий контекст

AI отримує додатковий контекст:

1. **AI Knowledge Base**
   - Пошук релевантних статей за запитом користувача
   - Використовує text search або regex fallback
   - До 3 найрелевантніших статей
   - Форматується як "ДОВІДКОВА ІНФОРМАЦІЯ З AI ЗНАНЬ"

2. **Тікети користувача**
   - Останні 5 тікетів користувача
   - Інформація: номер, заголовок, статус, дата створення
   - Використовується для відповідей про статус заявок

## Процес роботи AI

### Сценарій 1: Користувач описує проблему

1. Користувач пише: "Не працює принтер"
2. `handleAIChat()` викликає `analyzeIntent()`
3. AI аналізує намір → `isTicketIntent: true, confidence: 0.8`
4. Система автоматично:
   - Створює сесію з title="Не працює принтер"
   - Запускає процес створення тікета
   - Запитує детальний опис (якщо не визначено)

### Сценарій 2: Користувач запитує інформацію

1. Користувач пише: "Які мої заявки?"
2. `handleAIChat()` отримує контекст (тікети користувача)
3. `getAIResponse()` генерує відповідь на основі:
   - Системного промпту
   - Історії розмови
   - Контексту тікетів
4. Відправляє відповідь користувачу
5. Показує кнопки швидких дій

### Сценарій 3: Голосове повідомлення

1. Користувач надсилає голосове повідомлення
2. `handleVoice()` завантажує файл
3. `transcribeAudio()` розпізнає текст
4. Показує розпізнаний текст користувачу
5. Передає текст в `handleAIChat()` для обробки

## Налаштування

### Через BotSettings (таблиця `botsettings`):

- `groqApiKey` - API ключ Groq
- `aiEnabled` - увімкнути/вимкнути AI (boolean)
- `groqModel` - модель Groq (за замовчуванням: `llama-3.3-70b-versatile`)
- `aiSystemPrompt` - кастомний системний промпт (опціонально)

### Параметри AI:

- **Temperature**: 0.7 (для звичайних відповідей), 0.1 (для аналізу наміру)
- **Max tokens**: 1024
- **Top P**: 1
- **Stream**: false

## Обмеження та особливості

1. **Історія розмови**: Обмежена до 10 останніх повідомлень для економії токенів
2. **Контекст тікетів**: Тільки останні 5 тікетів користувача
3. **AI Knowledge Base**: До 3 найрелевантніших статей
4. **Fallback**: Якщо AI не може відповісти → показується головне меню
5. **Мова**: AI завжди відповідає українською, навіть якщо запит іншою мовою

## Логування

Всі операції AI логуються:
- Аналіз наміру з результатами
- Генерація відповідей
- Помилки транскрибації
- Помилки API Groq

## Залежності

- `groq-sdk` - офіційний SDK для Groq API
- `BotSettings` - модель для зберігання налаштувань
- `AIKnowledge` - модель для бази знань
- `Ticket` - модель для тікетів

