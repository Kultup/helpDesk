# Логіка роботи Telegram бота - Схема повідомлень

## 📍 Куди приходять повідомлення

### 📊 Швидкий довідник

#### 🎫 ТІКЕТИ:

| Подія | 👤 Користувач<br/>(приватний чат) | 👥 Група адмінів<br/>(Telegram) | 🔑 Кожному адміну<br/>(особисто) | 💻 Веб-панель<br/>(WebSocket) |
|-------|:---------------------------------:|:-------------------------------:|:--------------------------------:|:-----------------------------:|
| **Новий тікет створено** | ✅ Так*<br/>(тільки якщо створив через Telegram) | ✅ Так | ❌ Ні | ✅ Так |
| **Тікет → в роботі (in_progress)** | ✅ Так | ❌ Ні | ❌ Ні | ✅ Так |
| **Тікет → вирішено/закрито** | ✅ Так | ✅ Так | ❌ Ні | ✅ Так |
| **📊 Запит на оцінку<br/>(після закриття тікету)** | ✅ Так<br/>(Telegram/WebSocket/FCM*) | ❌ Ні | ❌ Ні | ✅ Так |

_*FCM = Firebase Cloud Messaging (push для мобільного додатку)_

#### 👥 КОРИСТУВАЧІ:

| Подія | 👤 Користувач<br/>(приватний чат) | 👥 Група адмінів<br/>(Telegram) | 🔑 Кожному адміну<br/>(особисто) | 💻 Веб-панель<br/>(WebSocket) |
|-------|:---------------------------------:|:-------------------------------:|:--------------------------------:|:-----------------------------:|
| **Нова реєстрація** | ❌ Ні | ❌ Ні | ❌ Ні | ✅ Так |
| **Реєстрація схвалена** | ✅ Так | ❌ Ні | ❌ Ні | ✅ Так |
| **Реєстрація відхилена** | ✅ Так | ❌ Ні | ❌ Ні | ✅ Так |
| **Зміна статусу користувача<br/>(активація/роль)** | ✅ Так | ❌ Ні | ✅ Так | ✅ Так |

#### 💼 ІНШЕ:

| Подія | 👤 Користувач<br/>(приватний чат) | 👥 Група адмінів<br/>(Telegram) | 🔑 Кожному адміну<br/>(особисто) | 💻 Веб-панель<br/>(WebSocket) |
|-------|:---------------------------------:|:-------------------------------:|:--------------------------------:|:-----------------------------:|
| **Запит на додавання посади** | ❌ Ні | ❌ Ні | ✅ Так | ✅ Так |

---

### 💡 Короткий підсумок:

**👤 КОРИСТУВАЧ (приватний чат з ботом) отримує:**
- ✅ Підтвердження створення тікету (ТІЛЬКИ якщо створив через Telegram бота)
- ✅ Автоматичні сповіщення про зміни статусів своїх тікетів
- ✅ Запит на оцінку якості (коли тікет закривається)
- ✅ Підтвердження/відхилення реєстрації
- ✅ Зміни свого статусу (активація/деактивація)
- ❌ НЕ отримує повідомлення від адмінів (спілкування тільки через веб-панель)

**👥 ГРУПА АДМІНІВ (Telegram група) отримує:**
- ✅ Новий тікет створено
- ✅ Тікет виконаний (resolved/closed)
- ❌ НЕ отримує зміни на in_progress

**🔑 КОЖЕН АДМІН ОСОБИСТО (Telegram) отримує:**
- ✅ Зміни статусу користувачів
- ✅ Запити на додавання посад
- ❌ НЕ отримує нові реєстрації (тільки веб-панель)

**💻 ВЕБ-ПАНЕЛЬ (WebSocket) показує:**
- ✅ Все - всі події в реальному часі
- ✅ Нові реєстрації (ТІЛЬКИ тут, не в Telegram!)
- ✅ Всі коментарі та спілкування по тікетах (Telegram бот НЕ підтримує спілкування)

---

### 🎯 Призначення Telegram бота:

**✅ ЩО РОБИТЬ БОТ:**
1. **Створення тікетів** - користувач може швидко створити тікет через Telegram
2. **Перегляд своїх тікетів** - список і деталі тікетів (тільки перегляд)
3. **Отримання сповіщень** - автоматичні сповіщення про зміни статусу, закриття тікету
4. **Оцінка якості** - запит на оцінку після закриття тікету
5. **Реєстрація** - можливість зареєструватися через бота

**❌ ЩО БОТ НЕ РОБИТЬ:**
1. **Спілкування з адмінами** - немає можливості відповідати або писати повідомлення
2. **Додавання коментарів** - тільки через веб-панель
3. **Отримання відповідей адміна** - адмін пише коментарі в веб-панелі, не в Telegram
4. **Двостороннє спілкування** - бот працює лише як інструмент для створення тікетів

**🔑 Ключова ідея:** Бот - це **швидкий спосіб створити тікет**, а **веб-панель - місце для спілкування та роботи з тікетом**.

---

### 1️⃣ **В ПРИВАТНИЙ ЧАТ З БОТОМ** (до користувача)

#### Коли користувач взаємодіє з ботом:
- ✅ **Підтвердження реєстрації** - користувачу приходить повідомлення про схвалення/відхилення реєстрації
- 🔔 **Зміна статусу тікету** - коли статус його тікету змінюється (автоматичні сповіщення системи)
- 📊 **Запит на оцінку якості** - коли тікет закривається (resolved/closed), автоматично відправляється запит на оцінку від 1 до 5 зірок
- 📝 **Відповіді бота** - на команди користувача (/start, /help, /tickets тощо)
- 🤖 **AI відповіді** - якщо увімкнено AI асистент (Groq)
- 🎫 **Інформація про тікети** - перегляд власних тікетів, деталі тікету
- 📋 **Шаблони тікетів** - перегляд та вибір шаблонів
- ⚡ **Статус змін користувача** - активація/деактивація акаунту, зміна ролі

#### ⚠️ ВАЖЛИВО - Призначення бота:
- ✅ **Бот призначений ТІЛЬКИ для створення тікетів** - користувач може створити тікет через бота
- ✅ **Автоматичні системні сповіщення** - користувач отримує сповіщення про зміну статусу, закриття тікету тощо
- ❌ **Спілкування через бота НЕ підтримується** - немає можливості відповідати адміну або спілкуватися
- ✅ **Всі коментарі та відповіді** - тільки через веб-панель в розділі "Тікети"
- 🎯 **Основна мета бота** - швидке створення тікету без заходу в веб-панель

**Функції:**
- `sendNotification(telegramId, notification)` - загальна функція для сповіщень
- `sendRegistrationApprovedNotification(user)` - підтвердження реєстрації
- `sendTicketNotification(ticket, type)` - сповіщення про зміни тікету
- `sendQualityRatingRequest(ticket)` - запит на оцінку після закриття тікету
- `handleRateTicketCallback(chatId, user, ticketId, rating)` - обробка оцінки від користувача
- `showUserDashboard(chatId, user)` - показати головне меню

**Налаштування:**
- Для приватних повідомлень використовується `user.telegramId` або `user.telegramChatId`

---

### 2️⃣ **В ГРУПУ АДМІНІСТРАТОРІВ** (Telegram група)

#### Коли приходять повідомлення в групу:
- 🎫 **Новий тікет створено** - сповіщення про кожен новий тікет (будь-який статус)
- ✅ **Тікет виконаний** - ТІЛЬКИ коли тікет отримує статус `resolved` або `closed`

#### ⚠️ НЕ приходять в групу:
- ❌ Зміна статусу на `in_progress` або інші проміжні статуси (тільки resolved/closed)
- ❌ Нові реєстрації користувачів (відображаються тільки в веб-панелі через WebSocket)
- ❌ Зміни статусів користувачів
- ❌ Запити на посади

**Формат повідомлень:**

```
🎫 Новий тікет створено
📋 [Назва тікету]
🏙️ [Місто] | 🆔 [ID тікету]
```

```
🎫 Тікет виконаний
📋 [Назва тікету]
🏙️ [Місто] | 🆔 [ID тікету]
```

**Функції:**
- `sendNewTicketNotificationToGroup(ticket, user)` - новий тікет
- `sendTicketStatusNotificationToGroup(ticket, previousStatus, newStatus, changedBy)` - зміна статусу **ТІЛЬКИ** на `resolved`/`closed`

**Код перевіряє:**
```javascript
if (newStatus === 'closed' || newStatus === 'resolved') {
  // Відправляємо в групу
} else {
  // Не відправляємо для in_progress та інших статусів
}
```

**Налаштування:**
- Використовується `TELEGRAM_GROUP_CHAT_ID` (змінна оточення)
- АБО `TelegramConfig.chatId` з бази даних (налаштування з адмін панелі)
- Пріоритет: спочатку перевіряється env, потім база даних

---

### 3️⃣ **АДМІНІСТРАТОРАМ ОСОБИСТО** (в приватний чат кожному адміну)

#### Коли приходять повідомлення окремим адмінам:
- 👤 **Зміна статусу користувача** - активація/деактивація, зміна ролі (через `userNotificationService.js`)
- 📝 **Запит на додавання нової посади** - коли користувач запитує додати посаду через бота

#### ⚠️ НЕ приходять адмінам в Telegram:
- ❌ Нові реєстрації користувачів (відображаються тільки в веб-панелі через WebSocket + можливо email)
- ❌ Зміни статусів тікетів (адміни бачать це в групі або веб-панелі)

**Формат повідомлень:**

```
🔧 Адмін сповіщення

Змінено статус користувача:
👤 [Ім'я Прізвище] ([email])

📋 Поле: [Назва поля]
📤 Було: [Старе значення]
📥 Стало: [Нове значення]

⏰ Час: [Дата та час]
```

```
📝 Новий запит на додавання посади

💼 Посада: [Назва]
👤 Telegram ID: [ID]
🆔 ID запиту: [ID]

Для додавання посади використайте адмін панель або API.
[Кнопки: ✅ Додати посаду | ❌ Відхилити]
```

**Функції:**
- `notifyAdminsAboutUserStatusChange(user, statusChanges)` - в `userNotificationService.js` - зміна статусів користувачів
- `notifyAdminsAboutPositionRequest(positionRequest, telegramId, positionName)` - в `telegramService.js` - запит на посаду

**Налаштування:**
- Шукає всіх користувачів з `role: 'admin'`, `super_admin` або `administrator`
- Які мають `isActive: true`
- Які мають `telegramId` (зв'язаний Telegram акаунт через `/start`)
- Перевіряє налаштування кожного адміна (`telegramSettings.notifications.statusUpdates`)

**Важливо:**
- Нові реєстрації НЕ відправляються в Telegram адмінам
- Використовується WebSocket для показу в реальному часі в веб-панелі (`registrationWebSocketService.js`)
- Адміністратори бачать нові реєстрації в розділі "Користувачі" веб-панелі

---

## 🌐 WebSocket сповіщення (веб-панель, НЕ Telegram)

### Що йде через WebSocket (в браузер користувача/адміна):

**Для адмінів:**
- 🆕 **Нові реєстрації користувачів** - показуються в реальному часі в веб-панелі
- 📊 **Оновлення лічильника очікуючих реєстрацій** - червоний значок з кількістю
- 🔄 **Зміна статусу реєстрації** - схвалення/відхилення

**Для користувачів:**
- 📊 **Запит на оцінку якості** - коли тікет закривається (якщо створено з веб-панелі)
- 🔔 **Сповіщення про зміни тікетів** - статуси, коментарі, призначення

### Файли:
**`backend/services/registrationWebSocketService.js`** - для адмінів
```javascript
// Відправка в кімнату admin-room (всім адмінам онлайн в браузері)
this.io.to('admin-room').emit('registration-notification', notification);
this.io.to('admin-room').emit('registration-count-update', notification);
```

**`backend/services/ticketWebSocketService.js`** - для користувачів
```javascript
// Відправка запиту на оцінку конкретному користувачу
ticketWebSocketService.notifyRatingRequest(userId, ticketData);
```

### Важливо:
- ⚠️ WebSocket сповіщення НЕ є Telegram сповіщеннями
- 💻 Працюють тільки коли адмін відкрив веб-панель в браузері
- 🔔 Для Telegram сповіщень про реєстрації потрібно додавати окремий код

---

## 🔧 Налаштування Chat ID для групи

### Варіант 1: Змінна оточення
```env
TELEGRAM_GROUP_CHAT_ID=-1001234567890
```

### Варіант 2: База даних (через адмін панель)
```javascript
TelegramConfig {
  key: 'default',
  botToken: '...',
  chatId: '-1001234567890',  // <-- Chat ID групи
  webhookUrl: '...'
}
```

### Як отримати Chat ID групи:
1. Додайте бота в групу
2. Надішліть будь-яке повідомлення в групу
3. Зайдіть на `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
4. Знайдіть `"chat":{"id":-1001234567890}` - це і є Chat ID групи
5. Chat ID груп завжди починається з `-` (мінус)

---

## 📊 Схема потоків повідомлень

```
┌─────────────────────────────────────────────────────────────┐
│                      КОРИСТУВАЧ                              │
│                                                               │
│  Створює тікет → sendMessage() → /start, /help, /tickets    │
│         ↓                                                     │
│    Приватний чат з ботом                                     │
│    ✅ Підтвердження дій                                      │
│    🔔 Статус тікету змінено                                  │
│    📋 Список тікетів                                         │
└─────────────────────────────────────────────────────────────┘
                              ↓
                    [Створено новий тікет]
                              ↓
        ┌─────────────────────┴─────────────────────┐
        ↓                                             ↓
┌──────────────────────┐                  ┌──────────────────────┐
│   ГРУПА АДМІНІВ      │                  │  КОЖНОМУ АДМІНУ      │
│   (Telegram група)   │                  │  (особисто)           │
│                      │                  │                      │
│ 🎫 Новий тікет       │                  │ 👤 Зміна статусу     │
│ ✅ Тікет виконаний   │                  │    користувача       │
│    (resolved/closed) │                  │ 📝 Запит на посаду   │
│                      │                  │                      │
│ ❌ НЕ приходять:     │                  │ ❌ НЕ приходять:     │
│ • in_progress        │                  │ • Нові реєстрації    │
│ • Нові реєстрації    │                  │   (тільки WebSocket) │
│                      │                  │                      │
│ chatId: -100123456   │                  │ telegramId: 123456   │
└──────────────────────┘                  └──────────────────────┘
```

---

## 📝 Приклади використання

### 1. Користувач створює тікет

#### 📱 Якщо створюється через Telegram бота:
```
Користувач → Приватний чат:
  "🎉 Тікет успішно створено!
   🆔 67890abcdef
   ⏳ Очікуйте відповідь адміністратора"

Група адмінів → Telegram:
  "🎫 Новий тікет створено
   📋 Не працює принтер
   🏙️ Київ | 🆔 67890abcdef"

Адміни (мобільний) → FCM push:
  "🎫 Новий тікет
   Створено новий тікет: Не працює принтер"

Веб-панель → WebSocket:
  [Реальний час - новий тікет з'являється в списку]
```

#### 💻 Якщо створюється через веб-панель або API:
```
Користувач → API Response:
  { success: true, message: "Тикет успішно створено", data: {...} }
  (НЕ отримує окремого Telegram або push сповіщення)

Група адмінів → Telegram:
  "🎫 Новий тікет створено
   📋 Не працює принтер
   🏙️ Київ | 🆔 67890abcdef"

Веб-панель → WebSocket:
  [Реальний час - новий тікет з'являється в списку]
```

⚠️ **Важливо:** Користувач отримує підтвердження створення тікету в Telegram **ТІЛЬКИ** якщо створює тікет через Telegram бота. При створенні з веб-панелі - тільки відповідь API.

### 2. Адмін змінює статус тікету на "Вирішено" або "Закрито"
```
Користувач → Приватний чат:
  "🔄 Статус тікету змінено
   📋 Не працює принтер
   ✅ Вирішено"

Група адмінів → Сповіщення:
  "🎫 Тікет виконаний
   📋 Не працює принтер
   🏙️ Київ | 🆔 67890abcdef"

⚠️ Примітка: Якщо адмін змінює статус на "В роботі" (in_progress),
повідомлення НЕ відправляється в групу, тільки користувачу.
```

### 3. Адмін активує нового користувача
```
Користувач → Приватний чат:
  [Показується головне меню бота]

Кожному адміну → Приватне повідомлення:
  "🔧 Адмін сповіщення
   Змінено статус користувача:
   👤 Іван Петренко (ivan@example.com)
   📋 Поле: Активність
   📤 Було: false
   📥 Стало: true"
```

### 4. Користувач запитує додати нову посаду через бота
```
Кожному адміну → Приватне повідомлення:
  "📝 Новий запит на додавання посади
   
   💼 Посада: Системний адміністратор
   👤 Telegram ID: 123456789
   🆔 ID запиту: 67890abcdef
   
   Для додавання посади використайте адмін панель або API.
   [Кнопки: ✅ Додати посаду | ❌ Відхилити]"
```

### 5. Адмін закриває тікет → користувач отримує запит на оцінку
```
Користувач → Приватний чат:
  "📊 Оцініть якість вирішення
   📋 Не працює принтер
   📊 ✅ Вирішено
   Оберіть оцінку від 1 до 5:
   [Кнопки: ⭐1 | ⭐⭐2 | ⭐⭐⭐3 | ⭐⭐⭐⭐4 | ⭐⭐⭐⭐⭐5]"

Після вибору оцінки:
  "✅ Дякуємо за оцінку!"

⚠️ Примітка: 
- Запит відправляється ТІЛЬКИ користувачу, який створив тікет
- Якщо тікет створено з Telegram → запит в Telegram
- Якщо тікет створено з веб-панелі → запит через WebSocket (або Telegram якщо є)
- Якщо тікет створено з мобільного додатку → запит через FCM push
- Запит відправляється ОДИН РАЗ при першому закритті тікету
```

---

## ⚙️ Налаштування сповіщень

### Для користувачів:
Налаштування зберігаються в `user.preferences.emailNotifications`:
- `newTickets` - нові тікети
- `assignedTickets` - призначені тікети
- `statusUpdates` - зміни статусу
- `weeklyReports` - тижневі звіти
- `systemUpdates` - системні оновлення

### Для адміністраторів:
Налаштування в `admin.telegramSettings.notifications`:
- `statusUpdates` - сповіщення про зміни статусів

---

## 🔍 Де знаходиться логіка

### Файли:
1. **`backend/services/telegramService.js`** - основна логіка бота (~5400 рядків)
   - Обробка команд користувачів
   - Відправка повідомлень в групу
   - Взаємодія з користувачами
   - `sendNewTicketNotificationToGroup()` - новий тікет в групу
   - `sendTicketStatusNotificationToGroup()` - тікет виконаний в групу (тільки resolved/closed)
   - `sendTicketNotification()` - сповіщення користувачу про зміну статусу
   - `sendQualityRatingRequest()` - запит на оцінку користувачу (Telegram/WebSocket/FCM)
   - `handleRateTicketCallback()` - обробка оцінки від користувача
   - `notifyAdminsAboutPositionRequest()` - запит на посаду кожному адміну

2. **`backend/services/userNotificationService.js`** - сповіщення користувачам (~350 рядків)
   - `sendUserStatusChangeNotification()` - сповіщення користувачу про зміни
   - `notifyAdminsAboutUserStatusChange()` - сповіщення кожному адміну про зміни користувача

3. **`backend/services/registrationWebSocketService.js`** - WebSocket сповіщення про реєстрації
   - Відправляє в `admin-room` (WebSocket, НЕ Telegram)
   - `notifyNewRegistration()` - нова реєстрація
   - `notifyRegistrationStatusChange()` - зміна статусу реєстрації
   - `updateRegistrationCount()` - оновлення лічильника

3.1. **`backend/services/ticketWebSocketService.js`** - WebSocket сповіщення про тікети
   - Відправляє в кімнату користувача (WebSocket)
   - `notifyRatingRequest()` - запит на оцінку користувачу
   - `notifyTicketUpdate()` - оновлення тікету

3.2. **`backend/services/fcmService.js`** - Firebase Cloud Messaging (push notifications)
   - Відправляє push сповіщення на мобільний додаток
   - `sendToUser()` - відправка сповіщення конкретному користувачу
   - `sendToAdmins()` - відправка сповіщення всім адмінам (при створенні тікету через Telegram)
   - Використовується для:
     - Запитів на оцінку з мобільного додатку
     - Сповіщень адмінам про нові тікети з Telegram
     - Сповіщень призначеним користувачам про нові тікети

4. **`backend/controllers/ticketController.js`** та **`backend/routes/tickets.js`** - управління тікетами
   - Виклик `sendNewTicketNotificationToGroup()` при створенні тікету
   - Виклик `sendTicketStatusNotificationToGroup()` при зміні статусу
   - Виклик `sendTicketNotification()` для повідомлення користувача
   - Виклик `sendQualityRatingRequest()` при закритті тікету (resolved/closed)

5. **`backend/controllers/userController.js`** - управління користувачами
   - Виклик `sendRegistrationApprovedNotification()` при схваленні
   - Виклик `sendRegistrationRejectedNotification()` при відхиленні

6. **`backend/models/TelegramConfig.js`** - конфігурація Telegram
   - Зберігання `botToken`, `chatId`, `webhookUrl`

---

## 🚀 Швидкий старт налаштування

### 1. Налаштуйте бота:
```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_GROUP_CHAT_ID=-1001234567890
```

### 2. Або через адмін панель:
- Зайдіть в **Налаштування → Telegram**
- Введіть Bot Token
- Введіть Chat ID групи (починається з `-`)
- Збережіть

### 3. Перевірте:
- Створіть тестовий тікет
- Перевірте чи прийшло повідомлення в групу
- Змініть статус тікету
- Перевірте чи прийшло повідомлення користувачу

---

## ❓ FAQ

**Q: Чому повідомлення не приходять в групу?**
A: Перевірте:
1. Чи встановлено `TELEGRAM_GROUP_CHAT_ID` або `TelegramConfig.chatId`
2. Чи додано бота в групу
3. Чи має бот права відправляти повідомлення в групі
4. Чи правильний Chat ID (починається з `-`)

**Q: Як змінити групу для сповіщень?**
A: Змініть `TELEGRAM_GROUP_CHAT_ID` в `.env` або `chatId` в налаштуваннях адмін панелі

**Q: Чому адміністратори не отримують особисті повідомлення (зміни статусів користувачів, запити на посади)?**
A: Перевірте:
1. Чи адміністратор зв'язав свій Telegram акаунт через бота (`/start`)
2. Чи у адміністратора є `telegramId` в базі даних
3. Чи увімкнені сповіщення в налаштуваннях адміністратора
4. Чи адміністратор має роль `admin` або `super_admin`
5. Чи акаунт адміністратора активний (`isActive: true`)

**Q: Як вимкнути сповіщення в групу?**
A: Видаліть `TELEGRAM_GROUP_CHAT_ID` з `.env` та `chatId` з `TelegramConfig` в базі даних

**Q: Чому я не бачу сповіщення про нові реєстрації в Telegram?**
A: Нові реєстрації НЕ відправляються в Telegram. Вони показуються:
1. В веб-панелі адміністратора в реальному часі через WebSocket
2. В розділі "Користувачі" → "Очікують підтвердження"
3. Можливо, через email (якщо налаштовано)

**Q: Чому адміни не отримують сповіщення про зміну статусу тікету на "В роботі"?**
A: Це задизайнено так. В групу відправляються сповіщення ТІЛЬКИ про:
1. Новий тікет (будь-який статус при створенні)
2. Тікет виконаний (статус `resolved` або `closed`)

Для інших змін статусів (`in_progress`, `open` тощо) сповіщення НЕ відправляються в групу.
Користувач отримує сповіщення про ВСІ зміни статусів свого тікету.

**Q: Як налаштувати сповіщення про всі зміни статусів в групу?**
A: Потрібно модифікувати код в `telegramService.js`, функція `sendTicketStatusNotificationToGroup()`:
```javascript
// Замість перевірки тільки на closed/resolved
if (newStatus === 'closed' || newStatus === 'resolved') {
  // відправити
}

// Зробити відправку для всіх статусів
const message = `🔄 Статус тікету змінено: ${newStatus}...`;
await this.sendMessage(groupChatId, message);
```

**Q: Куди приходить запит на оцінку якості?**
A: Запит на оцінку приходить ТІЛЬКИ користувачу, який створив тікет, коли тікет закривається:
- 📱 Якщо тікет створено з **Telegram** → запит приходить в **Telegram**
- 💻 Якщо тікет створено з **веб-панелі** → запит приходить через **WebSocket** (в браузері) або **Telegram** (якщо користувач зв'язав акаунт)
- 📲 Якщо тікет створено з **мобільного додатку** → запит приходить через **FCM push notification**

Запит НЕ приходить адмінам ні в групу, ні особисто. Тільки користувачу для оцінки якості роботи.

**Q: Як часто відправляється запит на оцінку?**
A: Запит відправляється ОДИН РАЗ при першому закритті тікету (статус `resolved` або `closed`). Якщо тікет знову відкриється і закриється, повторний запит НЕ відправляється.

**Q: Чому користувач не отримав підтвердження створення тікету в Telegram?**
A: Підтвердження в Telegram приходить ТІЛЬКИ якщо користувач створює тікет через Telegram бота. Якщо тікет створено через:
- 💻 **Веб-панель** → користувач бачить тільки API відповідь в браузері
- 📲 **Мобільний додаток** → користувач бачить тільки відповідь в додатку
- 🤖 **API** → тільки HTTP response

Група адмінів завжди отримує сповіщення в Telegram про новий тікет, незалежно від джерела створення.

**Q: Чи можна спілкуватися з користувачем через Telegram бота?**
A: ❌ НІ. Функціонал спілкування через бота було вимкнено. 
- **Бот призначений ТІЛЬКИ для створення тікетів** - користувач швидко створює тікет без заходу в веб-панель
- Користувач отримує **тільки автоматичні системні сповіщення** (зміна статусу, закриття тікету, запит на оцінку)
- Всі **коментарі та відповіді** адмінів і користувачів - **тільки через веб-панель**
- Користувач НЕ може відповідати адміну через Telegram
- Для спілкування використовуйте **веб-панель** → "Тікети" → коментарі до тікету

**Q: Чому при перегляді тікету в Telegram немає можливості відповісти?**
A: Це задумано. Функціонал відповідей через Telegram було видалено з коду.
- Бот показує деталі тікету **тільки для перегляду**
- Користувач може бачити статус, опис, пріоритет
- Для додавання коментарів потрібно використовувати **веб-панель**
- Це спрощує логіку бота та забезпечує єдину точку спілкування (веб-панель)

---

---

## 📋 Підсумок реальної логіки

### ✅ ЩО ПРАЦЮЄ:

**В Telegram групу адмінів:**
- Новий тікет створено ✅
- Тікет виконаний (resolved/closed) ✅

**Кожному адміну в Telegram особисто:**
- Зміна статусу користувача (активація/деактивація, роль) ✅
- Запит на додавання нової посади ✅

**Користувачу в Telegram:**
- Створення тікетів через бота ✅
- Автоматичні сповіщення про зміни статусів тікетів ✅
- Запит на оцінку якості (після закриття тікету) ✅
- Підтвердження/відхилення реєстрації ✅
- Відповіді бота на команди (/start, /help, /tickets) ✅
- AI асистент (Groq) ✅
- ❌ Спілкування з адміністраторами - ВИМКНЕНО (тільки веб-панель)

**В веб-панель (WebSocket):**
- Нові реєстрації в реальному часі ✅
- Зміни статусів ✅
- Всі події системи ✅

### ❌ ЩО НЕ ПРАЦЮЄ В TELEGRAM:

- ❌ **Спілкування через бота** - користувач НЕ може відповідати адміну, функціонал видалено з коду
- ❌ **Коментарі через Telegram** - всі коментарі тільки через веб-панель
- ❌ Зміна статусу тікету на `in_progress` НЕ йде в групу (тільки resolved/closed)
- ❌ Нові реєстрації користувачів НЕ йдуть ні в групу, ні адмінам (тільки WebSocket в веб-панель)
- ❌ Зміни статусів тікетів НЕ йдуть адмінам особисто в Telegram
- ❌ Запити на оцінку НЕ завжди йдуть в Telegram (залежить від джерела створення тікету):
  - Якщо тікет з Telegram → запит в Telegram ✅
  - Якщо тікет з веб-панелі → запит через WebSocket (або Telegram fallback) ⚠️
  - Якщо тікет з мобільного → запит через FCM push ⚠️

### 🔴 ВИДАЛЕНО З КОДУ (більше не працює):

- 🚫 `handleTicketReply()` - обробка відповідей користувача на тікет
- 🚫 `activeTickets` Map - відстеження активних тікетів для спілкування
- 🚫 `setActiveTicketForUser()` - встановлення активного тікету
- 🚫 `clearActiveTicketForUser()` - очищення активного тікету
- 🚫 Кнопка "Відповісти на тікет" - показує повідомлення, що функція недоступна
- 🚫 TelegramMessage модель для зберігання повідомлень (можливо, все ще є в коді, але не використовується)

### 💡 Як це змінити:

Якщо потрібно додати Telegram сповіщення про нові реєстрації адмінам:
1. Відкрити `backend/services/telegramService.js`
2. Додати функцію `notifyAdminsAboutNewRegistration(user)`
3. Викликати її в `backend/controllers/userController.js` після створення нової реєстрації

Якщо потрібно відправляти всі зміни статусів тікетів в групу:
1. Відкрити `backend/services/telegramService.js`
2. Знайти функцію `sendTicketStatusNotificationToGroup()`
3. Прибрати перевірку `if (newStatus === 'closed' || newStatus === 'resolved')`

---

**Дата створення:** 23.01.2026  
**Версія:** 1.1  
**Останнє оновлення:** 23.01.2026 - Виправлено відповідно до реального коду  
**Перевірено:** Всі функції перевірені в `telegramService.js`, `userNotificationService.js`, `registrationWebSocketService.js`
