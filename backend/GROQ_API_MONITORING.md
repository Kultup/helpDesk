# Моніторинг використання Groq API

## Огляд

Система автоматично відстежує використання Groq API та відправляє сповіщення адміністратору, коли ліміти наближаються до вичерпання.

## Функціональність

### 🔍 Автоматичне відстеження

Після кожного виклику Groq API система автоматично:
- ✅ Зберігає статистику використання (запити, токени, аудіо)
- ✅ Оновлює інформацію про поточні ліміти
- ✅ Перевіряє чи потрібно відправити сповіщення

### 📊 Що відстежується

#### Для моделі `llama-3.3-70b-versatile`:
- Кількість запитів
- Використані токени
- Час останнього запиту

#### Для моделі `whisper-large-v3`:
- Кількість запитів
- Використані секунди аудіо
- Час останнього запиту

#### Ліміти з API:
- Залишок запитів на день (RPD)
- Залишок токенів на хвилину (TPM)
- Час оновлення лімітів

### 🚨 Рівні сповіщень

Сповіщення відправляються на Telegram адміну (ID: `6070910226`):

#### ⚠️ Попередження (20% залишку)
Відправляється коли залишилось:
- ≤ 20% запитів на день
- АБО ≤ 20% токенів на хвилину

**Приклад:**
```
⚠️ Попередження: Ліміт Groq API

📊 Статус на сьогодні:
├ Залишилось запитів: 200 (20.0%)
├ Залишилось токенів: 2400 (20.0%)
└ Оновлення: 2h15m30s

🤖 Використання моделей:
├ LLaMA 3.3 70B: 800 запитів, 9600 токенів
└ Whisper: 15 запитів, 450 сек

💡 Слідкуйте за використанням API протягом дня.
```

#### 🚨 Критичне (5% залишку)
Відправляється коли залишилось:
- ≤ 5% запитів на день
- АБО ≤ 5% токенів на хвилину

**Приклад:**
```
🚨 КРИТИЧНО! Ліміт Groq API майже вичерпано!

📊 Статус на сьогодні:
├ Залишилось запитів: 50 (5.0%)
├ Залишилось токенів: 600 (5.0%)
└ Оновлення: 30m15s

🤖 Використання моделей:
├ LLaMA 3.3 70B: 950 запитів, 11400 токенів
└ Whisper: 48 запитів, 1440 сек

💡 Рекомендації:
• Розглянути покупку Developer Plan
• Тимчасово обмежити AI функції
• Перевірити ліміти: https://console.groq.com/settings/limits
```

### 📡 API Endpoints

#### GET `/api/groq-stats/current`
Отримати поточну статистику за сьогодні.

**Доступ:** Тільки адміністратори

**Відповідь:**
```json
{
  "success": true,
  "data": {
    "date": "2026-01-23T00:00:00.000Z",
    "modelUsage": {
      "llama-3.3-70b-versatile": {
        "requestsCount": 150,
        "tokensUsed": 18500,
        "lastRequest": "2026-01-23T14:30:00.000Z"
      },
      "whisper-large-v3": {
        "requestsCount": 25,
        "audioSecondsUsed": 750,
        "lastRequest": "2026-01-23T14:25:00.000Z"
      }
    },
    "rateLimits": {
      "remainingRequests": 850,
      "remainingTokens": 11500,
      "limitRequests": 1000,
      "limitTokens": 12000,
      "resetRequests": "9h30m15s",
      "resetTokens": "45s",
      "lastUpdated": "2026-01-23T14:30:00.000Z"
    },
    "notifications": {
      "lowRequestsNotified": false,
      "lowTokensNotified": false,
      "criticalNotified": false,
      "lastNotificationDate": null
    }
  }
}
```

#### GET `/api/groq-stats/history?days=7`
Отримати історію використання за останні N днів.

**Параметри:**
- `days` (опціонально, за замовчуванням 7) - кількість днів історії

**Доступ:** Тільки адміністратори

**Відповідь:**
```json
{
  "success": true,
  "data": [
    {
      "date": "2026-01-23T00:00:00.000Z",
      "modelUsage": { /* ... */ },
      "rateLimits": { /* ... */ }
    },
    // ... інші дні
  ],
  "period": {
    "days": 7,
    "from": "2026-01-16",
    "to": "2026-01-23"
  }
}
```

#### POST `/api/groq-stats/test-notification`
Відправити тестове сповіщення адміну.

**Доступ:** Тільки адміністратори

## Налаштування

### Зміна ID адміна для сповіщень

У файлі `backend/services/groqService.js`:

```javascript
class GroqService {
  constructor() {
    this.client = null;
    this.settings = null;
    this.adminTelegramId = '6070910226'; // ← Змінити тут
  }
  // ...
}
```

### Зміна порогів сповіщень

У файлі `backend/models/GroqApiUsage.js`, метод `shouldNotify()`:

```javascript
// Критичний рівень - 5%
if ((requestsPercentage <= 5 || tokensPercentage <= 5) && !this.notifications.criticalNotified) {
  return { level: 'critical', /* ... */ };
}

// Попередження - 20%
if ((requestsPercentage <= 20 || tokensPercentage <= 20) && !this.notifications.lowRequestsNotified) {
  return { level: 'warning', /* ... */ };
}
```

## База даних

### Модель: GroqApiUsage

```javascript
{
  date: Date,              // Дата (без часу)
  modelUsage: {
    'llama-3.3-70b-versatile': {
      requestsCount: Number,
      tokensUsed: Number,
      lastRequest: Date
    },
    'whisper-large-v3': {
      requestsCount: Number,
      audioSecondsUsed: Number,
      lastRequest: Date
    }
  },
  rateLimits: {
    remainingRequests: Number,
    remainingTokens: Number,
    limitRequests: Number,
    limitTokens: Number,
    resetRequests: String,
    resetTokens: String,
    lastUpdated: Date
  },
  notifications: {
    lowRequestsNotified: Boolean,
    lowTokensNotified: Boolean,
    criticalNotified: Boolean,
    lastNotificationDate: Date
  }
}
```

## Ліміти Groq API (Free Tier)

### llama-3.3-70b-versatile
- **RPM:** 30 запитів/хвилину
- **RPD:** 1,000 запитів/день
- **TPM:** 12,000 токенів/хвилину
- **TPD:** 100,000 токенів/день

### whisper-large-v3
- **RPM:** 20 запитів/хвилину
- **RPD:** 2,000 запитів/день
- **ASH:** 7,200 секунд/годину (2 години)
- **ASD:** 28,800 секунд/день (8 годин)

## Логування

Всі події відстеження API логуються:

```
[DEBUG] API usage tracked { model: 'llama-3.3-70b-versatile', tokensUsed: 150, remainingRequests: 850 }
[INFO] ⚠️ Сповіщення про ліміт API відправлено адміну { level: 'warning', requestsPercentage: '18.5' }
[ERROR] Помилка трекінгу API використання: ...
```

## Тестування

### 1. Перевірка автоматичного трекінгу
Просто використовуйте AI функції в боті - статистика оновиться автоматично.

### 2. Перегляд поточної статистики
```bash
curl -X GET "http://localhost:5000/api/groq-stats/current" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"
```

### 3. Тестове сповіщення
```bash
curl -X POST "http://localhost:5000/api/groq-stats/test-notification" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"
```

## Переваги

✅ **Проактивне управління** - дізнавайтесь про проблеми до їх виникнення  
✅ **Автоматизація** - нічого не треба налаштовувати вручну  
✅ **Історія** - відстежуйте тренди використання  
✅ **Гнучкість** - легко змінити пороги та адресатів сповіщень  
✅ **Інтеграція** - працює з існуючою Telegram інфраструктурою

## Майбутні покращення

- [ ] Dashboard з графіками використання
- [ ] Прогнозування вичерпання лімітів
- [ ] Автоматичне обмеження AI при наближенні до ліміту
- [ ] Email сповіщення як альтернатива Telegram
- [ ] Інтеграція з Developer Plan API для більших лімітів
